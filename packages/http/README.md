# HTTP Package

This is the official HTTP package for GlowLang. 

Use this to start a web server or make HTTP requests.

## Installation
`gdu install http`

## Usage
```glow
use "http"

let server = http.Server()
server.listen(8080)
```
